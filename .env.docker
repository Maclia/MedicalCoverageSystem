# ==========================================
# Medical Coverage System - Docker Environment
# ==========================================
# Quick configuration file for Docker deployments
# Copy this to .env.docker and update with your values

# --- Application Configuration ---
NODE_ENV=production
PORT=3001
HOST=0.0.0.0

# --- Database Configuration ---
POSTGRES_DB=medical_coverage
POSTGRES_USER=postgres
POSTGRES_PASSWORD=B0@2mAC254102892

# Database URL (auto-configured in docker-compose)
# DATABASE_URL will be set automatically by docker-compose

# --- Redis Configuration ---
REDIS_PASSWORD=B0@2mAC254102892
# REDIS_URL will be set automatically by docker-compose

# --- Security Configuration ---
# CRITICAL: Change these in production! Minimum 32 characters each
JWT_SECRET=408a7592e21f5c8bc3bdad3179607d85
JWT_REFRESH_SECRET=dc0adc1aba38c8d06a6f26b8a3405c7e
SESSION_SECRET=c135fda98892a11242cb88661f3c45393
ENCRYPTION_KEY=0c5fa05bf62dd3117a8e09f791917642

# JWT Expiration
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# --- CORS Configuration ---
CORS_ORIGIN=http://localhost:3001,http://localhost:3000
CORS_ENABLED=true
CORS_CREDENTIALS=true

# --- Feature Flags ---
ENABLE_TOKEN_SYSTEM=true
ENABLE_SCHEMES_BENEFITS=true
ENABLE_MEMBER_ONBOARDING=true
ENABLE_WELLNESS_PROGRAMS=true
ENABLE_DOCUMENT_UPLOAD=true
ENABLE_EMAIL_NOTIFICATIONS=true

# --- Token Management Configuration ---
DEFAULT_TOKEN_PRICE=0.50
TOKEN_EXPIRATION_ENABLED=false
TOKEN_EXPIRATION_DAYS=365
TOKEN_LOW_BALANCE_THRESHOLD=100
TOKEN_CRITICAL_BALANCE_THRESHOLD=50
TOKEN_AUTO_TOPUP_ENABLED=true

# --- Email Configuration (Optional) ---
# Uncomment and configure if you want email functionality
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# FROM_EMAIL=noreply@medicalcoverage.com
# FROM_NAME=Medical Coverage System

# --- File Upload Configuration ---
MAX_FILE_SIZE=10485760
ALLOWED_FILE_TYPES=jpg,jpeg,png,pdf,doc,docx
FILE_STORAGE_TYPE=local

# --- Logging Configuration ---
LOG_LEVEL=info
LOG_FORMAT=combined

# --- Rate Limiting ---
RATE_LIMIT_ENABLED=true
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# --- Database Backup (Optional) ---
BACKUP_ENABLED=false
BACKUP_SCHEDULE=0 2 * * *
BACKUP_RETENTION_DAYS=30

# --- Monitoring (Optional) ---
# SENTRY_DSN=your-sentry-dsn
# MONITORING_ENABLED=true
